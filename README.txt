arduino_sdl is a library that re-implements most of Arduino's interface on top
of SDL. The idea is to be able to test programs for arduino without needing to
first build a circuit or even having a board at all.

Note that this whole thing was written mostly for personal use.
